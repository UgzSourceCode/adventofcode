# @aoc/biome-config

Shared Biome configuration package used across the Advent of Code monorepo. It centralizes linting and formatting rules so applications can stay small and focused on puzzle code.

Used via `extends: ["@aoc/biome-config/base"]` in app‑level `biome.json` files.