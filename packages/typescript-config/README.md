# @aoc/typescript-config

Shared TypeScript configuration package used across the Advent of Code monorepo. It centralizes compiler options so applications can stay small and focused on puzzle code.

Used via `extends: ["@aoc/typescript-config/base"]` in app‑level `tsconfig.json` files.